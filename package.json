{
  "name": "tja-renderer",
  "version": "0.11.4",
  "description": "A high-level TJA chart renderer for the web.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "publishConfig": {
    "main": "dist/index.js",
    "types": "dist/index.d.ts"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "node --loader ts-node/esm tests/test-renderer.ts && node --loader ts-node/esm tests/test-auto-annotation.ts && node --loader ts-node/esm tests/test-pixel-snapping.ts"
  },
  "keywords": [
    "tja",
    "taiko",
    "renderer",
    "canvas"
  ],
  "author": "Jack",
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
